<h3>Task Management Application</h3>
<hr >

A task management application is built with Angular 4, ASP.NET Core and Entity Framework Core.

It needs to be opened in Visual Studio 2017 and Node.js needs to be installed.
The database connection is configured in appsettings.json. Using LocalDb by default and a database name TaskMgmt will be created if not exists.

